spring:
  application:
    name: food-service
  config:
    import: optional:configserver:${CONFIG_SERVER:http://localhost:8081}

aws:
  access:
    key: ${AWS_ACCESS_KEY:dummy-access-key}
  secret:
    key: ${AWS_SECRET_KEY:dummy-secret-key}
  region: ${AWS_REGION:us-east-1}

#spring:
#  datasource:
#    url: jdbc:postgresql://localhost:5432/postgres
#    username: rony
#    password: rony
#    driver-class-name: org.postgresql.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#
#  sql:
#    init:
#      mode: always
